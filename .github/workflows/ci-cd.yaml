name: CI/CD

on: push

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: pnpm/action-setup@v6
        with:
          run_install: true

      - uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"

      - name: Instrumentation
        run: |
          pwd
          ls -l
          cat tsconfig.json
          export
          node --version
          pnpm --version
          npx tsc --version

      - name: Build
        run: pnpm run build

      - name: Instrument folder
        run: |
          echo "ls -la dist/"
          ls -la dist/
          echo "cat dist/client/.assetsignore"
          cat dist/client/.assetsignore

      - name: Upload dist
        uses: actions/upload-artifact@v7
        with:
          path: dist/
          name: dist
          include-hidden-files: true # needed to ensure .assestignore is sent

      - name: Upload .wrangler
        uses: actions/upload-artifact@v7
        with:
          path: .wrangler/
          name: .wrangler
          include-hidden-files: true

  # we deploy in a separate job to be certain that we're not accidentally
  # uploading source code
  deploy:
    runs-on: ubuntu-latest
    # only if branch is main
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
      deployments: write
    name: Publish to Cloudflare Pages
    needs: build
    steps:
      - name: Download dist
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist

      - name: Download .wrangler
        uses: actions/download-artifact@v8
        with:
          name: .wrangler
          path: .wrangler

      - uses: pnpm/action-setup@v6
        with:
          version: latest

      # force the wrangler version be preinstalling it
      # https://github.com/cloudflare/wrangler-action/issues/390
      - name: Install wrangler
        run: pnpm add wrangler@4

      - name: Instrument folder
        run: |
          pwd
          echo "ls -la"
          ls -la
          echo "ls -la dist/"
          ls -la dist/
          echo "cat dist/client/.assetsignore"
          cat dist/client/.assetsignore

      - name: Publish to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
